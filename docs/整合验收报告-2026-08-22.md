# Project-008 三支线整合与最终 EXE 验收报告

- 日期：2026-08-22
- 自动化：`pnpm typecheck` PASS；Vitest 10 文件 / 41 项 PASS；`pnpm build` PASS；`pnpm package:win` PASS。
- 单实例：PASS。CDP `9275`，固定 userData，`launchCount=1`，结束后 `remaining=0`；只停止匹配标记的根 PID 树。
- EXE：`outputs/白音 AI 助手 0.1.0.exe`；78,561,179 bytes；2026-08-22T17:44:44.7854267+08:00；SHA-256 `9E6C832B5E06CC37F5C4E1D7D2E6A273EEEB788DF69992201E8F2F19BB23F3E2`。

## 通过

- 表现配置已由主进程 settings 持久化；SettingsWindow 仅发送 `baoyin:settings-preview` 的 presentation 语义域；PetWindow 唯一 Live2D controller 即时收到。实测 physics `true -> preview false -> persisted true`，五项值持久化。
- 三组窗口尺寸只改变裁剪视口；canvas CSS basis、modelScale、offset 均保持；X/Y/scale 单变量和 5 秒按住不缩放通过。
- SettingsWindow 为 frameless，菜单已移除；设置首页、人格、实时表现、模型构图、窗口交互均有真实 renderer 截图；拖动区为 drag，控件为 no-drag。
- 真实 User32 光标连续帧与透明角点击穿透通过；下层窗口收到点击。
- 创建空白角色与复制角色成功，独立 ID；切换后 system/personality 快照不同；默认角色删除保护和角色包无 Key 通过。
- 外部 Miku 只读加载，6/6 纹理、440 drawables、40 组 physics；EXE 未包含 D 盘模型。
- 七条实际 exp3 路由逐项触发并记录 8 帧 timeline 与截图；原生水印参数实测 `0 -> 1 -> 最终 0`，关闭/恢复均通过，未改模型文件。
- gaze/头部/身体输入与官方 updater 顺序、连续帧、timeline 已采集；没有直接写 physics 输出。

## 未通过或未完成验证

- Scene1/greet 能启动，但模型 motion 文件声明 loop，4 秒后仍为 active；“动作结束自动恢复注视/情绪”本轮 FAIL，不能冒充通过。
- physics updater 存在，但本轮抽样的六个 physics output 值为 0；快速左右甩动造成发丝/服饰物理甩动与自然回弹未获得充分数值/视频证据，FAIL/未验证。
- blink、breath 只确认官方 updater 在运行，未取得对应参数完整周期序列，未验证。
- SettingsWindow 原生窗口拖动、滚轮/键盘滚到底没有 OS 级证据；仅有样式、页面与 renderer 操作证据，未验证。
- 角色导入/导出系统文件对话框、删除自定义角色、退出后重新启动恢复未执行；严格 `launchCount=1` 与“重启验证”互相冲突，本轮保持单实例约束并明确未验证。
- 多屏、DPI、虚拟桌面未验证。
- 真实 User32 模型编辑中移动矩阵变化通过，但滚轮帧的矩阵变化检查为 false；独立 CDP 变换回归的 scale 单变量通过，因此 OS 滚轮视觉证据仍按未完全验证处理。

## 证据

- `outputs/integration-final-single-instance.json`
- `outputs/integration-final-window/window-transform-cdp.json`
- `outputs/integration-final-cursor/cursor-interaction-check.json`
- `outputs/integration-final-runtime/integration-final-runtime.json`
- `outputs/integration-final-gui-model/gui-evidence.json`
- 对应目录中的连续帧与 PNG 截图。
